{
  "name": "postgres-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MCP server for read-only Postgres exploration + optional insert helper",
  "bin": {
    "postgres-mcp": "./src/server.mjs"
  },
  "scripts": {
    "start": "node src/server.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "pg": "^8.13.1"
  }
}
